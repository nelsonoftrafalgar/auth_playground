import { Button } from '@repo/ui/Button'
import { Card } from '@repo/ui/Card'
import { Header } from '@repo/ui/Header'

export const Main = () => {
	return (
		<Card>
			<Header>Access granted</Header>
			<Button>LOGOUT</Button>
		</Card>
	)
}
